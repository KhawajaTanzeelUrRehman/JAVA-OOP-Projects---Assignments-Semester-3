/*
 * The Circle class models a circle with a radius and color.
 */
/*public class Circle {  
   // private instance variable, not accessible from outside this class
   private double radius;
   private String color;
   //static variable to count the number of objects.
   static int noOfObjects;
   
   // The default constructor with no argument.
   // It sets the radius and color to their default value.
   public Circle() {
      radius = 1.0;
      color = "red";
      //Increment the number of object by 1.
      noOfObjects++;
   }
   
   // 2nd constructor with given radius, but color default
   public Circle(double r) {
      radius = r;
      color = "red";
      //Increment the number of object by 1.
      noOfObjects++;
   }
   // 3rd constructor with given radius and given color
   public Circle(double r , String c) {
      radius = r;
      color = c;
      //Increment the number of object by 1.
      noOfObjects++;
   }
   
   // A public method for retrieving the radius
   public double getRadius() {
     return radius; 
   }
   // A public method for retrieving the color
   public String getColor()
   {
       return color;
   }
   
   // A public method for computing the area of circle
    public double getArea() {
        return radius*radius*Math.PI;
   }
    //A method to set the value of the radius.
    public void setRadius(double r)
    {
        radius = r;
    }
    //A method to set the value of the color.
    public void setColor(String c)
    {
        color = c;
    }
}
*/